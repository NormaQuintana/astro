---
const tasks = [
  "Estudiar Astro",
  "Hacer la tarea de matematicas",
  "Preparar la presentacion",
  "Leer un libro",
];
---
<html>
  <head>
    <title>Mis Tareas Pendientes</title>
    <style>
      /* Estilos para el botón */
      .big-red-button {
        background-color: red;
        color: white;
        font-size: 20px;
        padding: 15px 30px;
        border: none;
        cursor: pointer;
        border-radius: 10px;
        margin-top: 20px;
      }
      .big-red-button:hover {
        background-color: darkred;
      }
      /* Estilos para el mensaje */
      .message {
        margin-top: 20px;
        font-size: 24px;
        color: green;
      }
    </style>
  </head>
  <body>
    <h1>Mis Tareas Pendientes</h1>
    <ul>
      {tasks.map((task) => (
        <li>{task}</li>
      ))}
    </ul>

    <!-- Botón rojo -->
    <button class="big-red-button" id="helloButton">Haz clic aqui</button>

    <!-- Botón Norma -->
    <button class="big-red-button" id="NormaButton">Mi nombre</button>

    <!-- Mensaje "Mi nombre" -->
    <div id="message" class="message" style="display: none;">
      Norma Isabel Torales Quintana
    </div>

    <!-- Mensaje "Hola, mundo" -->
    <div id="messageContainer" class="message" style="display: none;">
      ¡Hola, mundo!
    </div>

    <!-- Script para manejar el clic del botón -->
    <script>
      const button = document.getElementById("helloButton");
      const messageContainer = document.getElementById("messageContainer");
      const button2 = document.getElementById("NormaButton");
      const message2 = document.getElementById("message");

      button.addEventListener("click", () => {
        messageContainer.style.display = "block"; // Muestra el mensaje
      });
      button2.addEventListener("click", () => {
        message2.style.display = "block"; // Muestra el mensaje
      });
    </script>
  </body>
</html>